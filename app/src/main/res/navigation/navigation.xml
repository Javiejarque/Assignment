<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/welcome">

    <fragment
        android:id="@+id/welcome"
        android:name="com.example.assignment.Welcome"
        android:label="welcome_fragment"
        tools:layout="@layout/welcome_fragment" >
    <action
        android:id="@+id/action_welcomeFragment_to_nextFragment"
        app:destination="@id/nextFragment" />
    </fragment>
    <fragment
    android:id="@+id/nextFragment"
    android:name="com.example.assignment.NextFragment"
    android:label="next_fragment"
    tools:layout="@layout/next_fragment" />
</navigation>